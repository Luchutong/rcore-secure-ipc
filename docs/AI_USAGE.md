---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: 'e7216a48-ea08-4be2-bc63-3601c2056b91'
  PropagateID: 'e7216a48-ea08-4be2-bc63-3601c2056b91'
  ReservedCode1: '45b64d68-74fa-4a20-ad6e-6976a003bc93'
  ReservedCode2: '45b64d68-74fa-4a20-ad6e-6976a003bc93'
---

# AI 工具使用记录

本项目按照课程要求记录 AI 工具的使用情况，后续应持续补充具体日期、任务、采用内容和人工验证结果。

## 已使用工具

- 工具：OpenAI Codex
- 使用场景：课题调研、rCore 与 xv6 IPC 安全机制对比、项目范围设计、仓库初始化和文档草拟。
- 人工责任：所有设计决策、代码修改、测试结果与报告结论均需由项目成员审阅、运行和验证。

## 记录模板

| 日期 | 使用者 | AI 工具 | 任务 | AI 建议或产出 | 是否采用 | 人工验证 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-08-31 | 项目组 | OpenAI Codex | 选题与仓库初始化 | IPC 安全缺口分析、开发路线图和初始文档 | 部分采用 | 待项目组复核 |
| 2026-08-31 | 项目组 | OpenAI Codex | CI 修复与四人协作设计 | 定位 `gh-pages` 写权限故障，拆分只读验证与主分支发布任务，草拟 API v1、职责分工和合并流程 | 已采用 | YAML 与本地差异检查通过，等待 GitHub Actions 验证 |
| 2026-08-31 | 项目组 | OpenAI Codex | 模块独立性审计 | 分析任务、系统调用、管道和用户内存依赖，建立安全模块骨架、进程安全状态、文件所有权和分支基线 | 已采用 | 静态冲突面检查完成，等待 integration CI 验证 |
| 2026-09-04 | 开发者 D | OpenAI Codex | 审计 ABI v1 设计 | 设计固定宽度审计记录、统计结构、非破坏性游标读取、溢出标志、权限与 errno 契约 | 已采用 | 人工核对现有 API、RV64 三参数系统调用约束与模块边界；实现和运行测试待后续完成 |
| 2026-09-05 | 开发者 D | OpenAI Codex | 审计事件转换与 errno 映射 | 在现有环形缓冲区上实现稳定操作/错误码映射、80 字节 ABI 输出、控制调用失败记录和中文注释；补充复用内核源码的主机测试 | 已写入工作区，待人工审阅 | 待人工复核；自动验证：7 项主机测试、内核构建、文档构建及 21 个正常/4 个预期异常用户用例通过；602/603 接线和跨模块测试尚未完成 |
| 2026-09-05 | 开发者 D | OpenAI Codex | 内核 602/603 系统调用主体 | 实现身份快照、读取权限、参数与输出范围检查、非破坏性批量读取、统计复制、统一失败审计和中文说明；复用审计模块并新增 9 项主体测试 | 已写入工作区，待人工审阅 | 待人工复核；自动验证：原有 7 项与新增 9 项主机测试、RV64 构建、文档构建、格式检查和 21 个正常/4 个预期异常用户用例通过；全局分发、用户库和 B 的真实地址安全仍待后续集成 |
| 2026-09-05 | 开发者 D | OpenAI Codex | 用户态审计 API | 设计并实现 ABI v1 用户结构、602/603 底层封装、切片读取与统计接口、未知操作兼容辅助函数及隔离的原始测试入口 | 已写入工作区，待人工审阅 | 待人工复核；自动验证：用户库格式/文档/RV64 构建、完整内核构建、16 项审计主机测试和 21 个正常/4 个预期异常 QEMU 用例通过；内核总路由和真实用户态调用待下一提交 |

2026-09-05，开发者 D 使用 OpenAI Codex 完成 `auditctl` 设计与实现、602/603 路由接线、
工具主机测试及 QEMU 用户态回归，补充 [工具设计文档](AUDITCTL.md)。已写入工作区，待人工审阅。
自动验证：12 项工具测试与原有 16 项主机测试通过；QEMU 的 `auditctl_test` 和原有用户测试
通过，共 22 个正常、4 个预期异常用例。人工验证尚待成员执行；A/B/C 联合安全验收不在本次结论内。

2026-09-05，开发者 D 使用 OpenAI Codex 整理 [游标、覆盖与统计测试设计](AUDIT_TEST_DESIGN.md)。
产出包括基于非法 `ipc_stat` 参数的独立事件源、游标/覆盖/统计用例矩阵、精确增量公式、
主机与 QEMU 验证边界、有效输出缓冲区和可重复执行方案。设计已写入工作区，新增测试尚未实现。
自动核对：原有 28 项主机测试通过；本次没有执行新增用例或 QEMU。人工审阅与后续实现待完成。

2026-09-05，开发者 D 使用 OpenAI Codex 落实 `user/src/bin/audit_test.rs` 并登记到用户测试入口。
实现18个用户态游标/覆盖/统计场景、页内静态输出及哨兵、精确增量和有界分页，补充实现记录。
自动验证：同一次QEMU启动连续运行两遍通过；完整回归23个正常/4个预期异常用例通过；
临时失败断言使audit_test退出1、make退出2，恢复后回归通过；原有28项主机测试、用户格式和
Clippy检查通过。未新增独立主机生命周期测试，A/B/C联合验收与人工复核仍待完成。

2026-09-09，开发者 D 使用 OpenAI Codex 核对四个远程功能分支并提前合并 C 的资源治理实现。
联调修正管道创建的审计数量与内部端点配额语义，调整 `auditctl_test` 对真实捕获管道事件的
预期，并新增成功创建、`ENOSPC`、统计增量、失败回滚和关闭恢复测试。自动验证：D 独立分支
28项宿主测试及27/27项QEMU测试通过，PR #1 GitHub Actions通过；C+D预览分支28项宿主测试
及29/29项QEMU测试通过。人工仍需审阅代码和测试结论；A/B联合测试、C的管道读写审计与
性能对比尚未完成。详细证据见 [角色 D 进度与验证记录](D_VERIFICATION.md)。

2026-09-09，开发者 D 使用 OpenAI Codex 增加手工 IPC 性能基准，并在原始 `main`、D 未接线
对照组与 C+D 安全路径上各运行5次、每次20,000轮管道创建/关闭。三组中位数分别为647、
674和662 ms；安全路径每次新增20,000条成功审计事件。安全路径相对原始基线初测增加约
2.3%，但D未接线组的反向差异显示QEMU和宿主调度噪声不可忽略，因此不将2.3%解释为精确
开销。方法、原始数据和最终验收限制见 [IPC 安全路径性能初测](PERFORMANCE.md)，仍待成员
人工复核。

2026-09-09，开发者 D 使用 OpenAI Codex 分析同一提交两组 GitHub Actions 一过一败的问题。
失败日志显示 `user_sig_test_multiprocsignals` 中子进程可能在父进程 `kill` 前退出。修复加入
管道就绪握手和原子信号接收标志，不使用重试掩盖竞态。自动验证：`sig_tests` 在同一次QEMU
启动中连续10次通过，随后完整QEMU 23个正常与4个预期失败用例共27/27通过；push与PR两条
远端CI均通过，仍待成员人工复核。

2026-09-09，开发者 D 使用 OpenAI Codex 增加多进程审计压力回归。6个子进程共制造768条
确定的`EINVAL`事件，验证精确统计增量、有界保留、覆盖计数、连续序号、`GAP_BEFORE`、
时间戳和PID归属；测试通过改进后的`until_timeout`设置10秒上限并传播子进程退出码。
自动验证：两次压力本体13～18 ms、含包装器35～42 ms，完整QEMU 24个正常与4个预期失败
用例共28/28通过；临时故障注入可使子进程、包装器、`usertests`和`make`逐层失败；另以
`infloop`和100 ms上限确认`SIGKILL`终止、回收并返回shell，同时修复旧实现误传信号位掩码
导致永久等待的问题。C+D预览分支的宿主测试28/28、QEMU 26个正常与4个预期失败用例
共30/30通过，配额和审计统计未受影响。D分支push与PR流水线及C+D预览流水线的文档、
QEMU作业均通过；成员人工复核仍待完成，A/B/C正式集成后的联合压力场景不在本次结论内。

|2026‑09‑10|	|开发者 B|	|deepseek|	|用户指针内存安全模块开发|	|修复以下问题：未映射地址 panic（DoS）、无 `\0` 字符串导致的无限循环、`ptr+len` 溢出回绕、不检查 `U` 标志可触达内核页、不检查 R/W 权限可写只读段、跨页结构体读到物理不连续数据、argv 数组无终止符无限遍历、非法 open flags panic—— 共 8 类。|	已采用	|完成编译，通过基础测试；等待项目组复核
| 2026-09-03 | 于泽通 | TeleAgent (星辰超级智能体) | 角色A：进程凭据与授权策略实现 | 实现 `Credentials` 结构与 UID 分配器、`authorize_signal` 五级权限检查、重写 `sys_kill` 走 IPC 授权链路、新增 `sys_getuid`/`sys_setuid` 系统调用、编写用户态测试程序 `cred_test.rs` | 已采用 | 内核+用户程序编译通过，QEMU 运行 5/5 测试 PASS，exec 不改 UID 行为确认 |
| 2026-09-13 | 项目组 | OpenAI Codex | A/B/C/D 四模块集成 | 按 B→A→C→D 依赖完成正式祖先合并，统一用户复制、凭据授权、配额回滚和审计路径；补充坏指针、跨 UID 信号、管道读写与跨页 ABI 输出测试 | 已采用，待成员审查 | 本地宿主测试 28/28、QEMU 32/32 通过；远程预览分支 CI 通过，待 integration PR 审查 |
| 2026-09-14 | 项目组 | OpenAI Codex | 稳定版合并与项目收尾 | 创建并合并 `integration` 到 `main` 的最终 PR，核对合并后 CI，执行最终性能复测，并整理 README、路线图和最终验收记录 | 已采用 | 最终 PR 与 `main` 合并后 CI 均成功；宿主测试 28/28、QEMU 32/32 通过；文档发布成功；性能复测五次中位数 746 ms，原始数据已归档，仍需项目成员最终审阅 |
| 2026-09-14 | 项目组 | OpenAI Codex | 最终成果交付材料 | 基于 `v1.0.0` 代码和真实测试日志整理用户程序说明、课程设计报告草稿、四人任务单、截图规范、答辩 PPT 逐页材料及白底论文式数据图；生成可编辑 DOCX 和交付校验器 | 已采用，待成员排版复核 | 材料分支自动校验通过：必需文件、10 个用户程序、6 组 SVG/PNG、4 份日志、DOCX 压缩结构和本地链接均有效；测试数字来自 main CI #34799749443 与本地复测 |

## 使用原则

- 不把未经验证的 AI 输出直接作为实验结论。
- 在提交信息、设计文档和答辩材料中标明 AI 参与部分。
- 保留关键交互记录，说明人工修改和验证过程。
- 不向 AI 工具提交密钥、口令或其他敏感信息。

> AI生成
